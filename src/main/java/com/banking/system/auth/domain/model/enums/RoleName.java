package com.banking.system.auth.domain.model.enums;

/**
 * Enumeration of available role names in the system.
 * Used as identifiers for roles stored in the database.
 */
public enum RoleName {
    CUSTOMER,
    ADMIN,
    BRANCH_MANAGER,
    SUPPORT,
    COMPLIANCE,
    AUDITOR
}